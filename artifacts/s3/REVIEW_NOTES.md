# Review Notes for s3

Generated: 2026-02-27T14:19:17.673Z

Total Fields: 40
CONFLICT: 4
REVIEW_REQUIRED: 14

## CONFLICT Items

- [ ] gb per month :: Data returned by S3 Select Value (NUMBER)
  note: Very low confidence score.
- [ ] gb per month :: Data scanned by S3 Select Value (NUMBER)
  note: Very low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: PUT, COPY, POST, LIST requests to S3 One Zone-IA Enter amount of requests (TEXT)
  note: Very low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: PUT, COPY, POST, LIST requests to S3 Glacier Instant Retrieval Enter amount of requests (TEXT)
  note: Very low confidence score.

## REVIEW_REQUIRED Items

- [ ] s3 standard info: :: Description - optional (TEXT)
  note: Low confidence score.
- [ ] s3 standard info: :: PUT, COPY, POST, LIST requests to S3 Standard Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] s3 standard info: :: GET, SELECT, and all other requests from S3 Standard Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] gb per month :: PUT, COPY, POST, LIST, RENAME requests to S3 Express One Zone Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] gb per month :: GET and all other requests from S3 Express One Zone Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: PUT, COPY, POST, LIST requests to S3 INT Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: GET, SELECT, and all other requests from S3 INT Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: Lifecycle Transition requests Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: GET, SELECT, and all other requests from S3 One Zone-IA Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: PUT, COPY, POST, LIST requests to S3 Glacier Deep Archive Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: Lifecycle Transitions into S3 Glacier Deep Archive Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: Restore requests (Standard) Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: Restore requests (Bulk) Enter amount of requests (TEXT)
  note: Low confidence score.
- [ ] the specified amount of data is already stored in s3 standard :: GET, SELECT, and all other requests from S3 Glacier Instant Retrieval Enter amount of requests (TEXT)
  note: Low confidence score.

- [ ] Budget was hit (`budget_hit: true`). Some states may have been skipped.
